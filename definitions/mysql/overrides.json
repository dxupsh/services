{
  "service": "mysql",
  "settings": {
    "user": {
      "value": "dxup",
      "rationale": "Scratch images use a single embedded user"
    },
    "group": {
      "value": "dxup",
      "rationale": "Scratch images use a single embedded user"
    },
    "configFile": {
      "value": null,
      "rationale": "Must provide config with bind-address = 0.0.0.0; no bind address exposed in settings"
    }
  }
}

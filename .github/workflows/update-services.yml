name: Update Services

on:
  workflow_call: # Called by release.yml
  workflow_dispatch: # Allow manual trigger

concurrency:
  group: update-services
  cancel-in-progress: false

permissions: {}

jobs:
  update-services:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Nix
        uses: DeterminateSystems/nix-installer-action@main

      - name: Setup Nix cache
        uses: DeterminateSystems/magic-nix-cache-action@main

      - name: Extract options
        run: bash scripts/extract-options.sh

      - name: Merge settings
        run: bash scripts/merge-settings.sh

      - name: Validate settings
        run: nix develop -c bash scripts/validate-settings.sh

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add definitions/ release/
          if git diff --cached --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Update service settings"
            git push
          fi
